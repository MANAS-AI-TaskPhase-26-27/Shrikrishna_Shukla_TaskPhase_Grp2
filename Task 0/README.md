## Task 0

## 0.1 ubuntu installation:

### What I learned

- learned how to install ubuntu and dual boot.
- basic linux terminal usage and basic commands like `cd`, `ls`, `pwd`, `mkdir`, `cp`, `mv`, and `rm`.



### Issues faced

- since ubuntu was set as first boot option in the boot order, the system automatically boots into ubuntu when i dont select anything. this is especially inconvenient during windows updates when system repeatedly restarts a couple of times.

---

## Task 0.2 GIT:

- learned basics of Git and GitHUB, and how they are used as version control system.
- learned git workflow. 
- learned basic git commands such as 
```
bash 
git init 
git status
git add
git commit 
git branch 
git checkout 
git merge 
git rebase 
git clone 
git push
git pull
git fetch
git restore
git reset
git stash
git revert
```


---


## Task 0.3 Python

> I had no prior practical experience with Python, so I started learning it from the basics. I learned basic syntax, variables and data types, input/output, conditional statements, loops and functions. I also worked with strings, lists, tuples and dictionaries. I started learning Object-Oriented Programming and got familiar with classes, objects, methods and `__init__`. I practiced these concepts by solving programming problems on HackerRank and used the errors I encountered along with my notes to improve my understanding. Due to being unwell during the task period, I was unable to get to NumPy and Pandas.
